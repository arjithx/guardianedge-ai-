'use client';
import { PhoneCall, MonitorUp, ClipboardPaste, ShieldCheck } from 'lucide-react';
import { useBanking } from '@/context/BankingContext';
const switches = [{key:'activeCall',label:'Simulate Active Scam Call',icon:PhoneCall},{key:'screenShare',label:'Remote Screen Share (AnyDesk)',icon:MonitorUp},{key:'clipboard',label:'Clipboard-Pasted Beneficiary',icon:ClipboardPaste}] as const;
export function DemoControlBar(){ const {telemetry,setTelemetry}=useBanking(); return <div className="sticky top-0 z-50 border-b border-blue-100 bg-navy px-4 py-3 text-white"><div className="mx-auto flex max-w-7xl flex-wrap items-center gap-3"><div className="mr-3 flex items-center gap-2 font-extrabold"><ShieldCheck className="text-teal"/> GuardianEdge <span className="text-teal">AI</span></div><span className="text-xs font-bold text-blue-200">DEMO SIMULATION</span>{switches.map(({key,label,icon:Icon})=><button key={key} onClick={()=>setTelemetry({...telemetry,[key]:!telemetry[key]})} className={`flex items-center gap-2 rounded-full px-3 py-2 text-sm font-bold transition ${telemetry[key]?'bg-coral text-white':'bg-white/10 text-blue-100 hover:bg-white/20'}`}><Icon size={16}/>{label}: {telemetry[key]?'ON':'OFF'}</button>)}</div></div> }
