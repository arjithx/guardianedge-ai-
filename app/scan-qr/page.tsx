'use client';
import { useRef, useState } from 'react';
import { useRouter } from 'next/navigation';
import { Camera, ScanLine } from 'lucide-react';
export default function ScanQr(){const router=useRouter();const video=useRef<HTMLVideoElement>(null);const [camera,setCamera]=useState(false);const start=async()=>{try{const s=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'}});if(video.current)video.current.srcObject=s;setCamera(true)}catch{setCamera(false)}};const simulate=(unknown:boolean)=>{sessionStorage.setItem('trustpay-qr',JSON.stringify(unknown?{beneficiary:'Unknown Verification Desk',upi:'verify.now@trustpay',amount:'50000',remarks:'urgent electricity penalty'}:{beneficiary:'Apollo Hospital',upi:'apollo.pharmacy@trustpay',amount:'80000',remarks:'Medical emergency'}));router.push('/transfer')};return <main className="min-h-screen bg-slate-950 p-5 text-white"><div className="mx-auto max-w-md"><button onClick={()=>router.push('/')} className="text-sm font-bold text-indigo-200">← Back</button><h1 className="mt-5 text-2xl font-black">Scan & Pay</h1><div className="relative mt-6 aspect-square overflow-hidden rounded-3xl border-2 border-indigo-400 bg-slate-900">{camera?<video ref={video} autoPlay playsInline className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center text-center text-slate-400"><Camera size={52}/><p>Camera preview is ready when permission is granted.</p></div>}<div className="absolute inset-10 rounded-2xl border-2 border-white"><ScanLine className="absolute left-4 right-4 top-1/2 w-[calc(100%-2rem)] animate-pulse text-cyan-300"/></div></div><button onClick={start} className="mt-5 w-full rounded-xl bg-indigo-600 py-4 font-black">Open camera scanner</button><p className="mt-7 text-sm font-bold text-slate-300">Desktop fallback</p><div className="mt-3 grid gap-3"><button onClick={()=>simulate(false)} className="rounded-xl bg-emerald-600 p-4 text-left font-bold">Simulate Apollo Hospital QR<br/><span className="text-sm font-normal">Verified medical merchant · INR 80,000</span></button><button onClick={()=>simulate(true)} className="rounded-xl bg-rose-600 p-4 text-left font-bold">Simulate unknown QR<br/><span className="text-sm font-normal">Unverified recipient · INR 50,000</span></button></div></div></main>}
